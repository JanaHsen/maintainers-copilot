"""One-shot bootstrap utilities run before the api accepts requests.

Currently contains the admin-bootstrap step that creates the first admin
maintainer at initial stack-up so the admin panel (Part 3) is reachable.
"""
